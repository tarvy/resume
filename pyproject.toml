[project]
name = "resume"
version = "0.1.0"
description = "Resume conversion tooling"
requires-python = ">=3.12"
dependencies = [
    "python-docx>=1.2.0",
    "pyyaml>=6.0.3",
    "weasyprint>=67.0",
]

[project.scripts]
resume = "convert_resume:main"
